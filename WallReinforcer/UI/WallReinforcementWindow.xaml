<Window x:Class="WallReinforcer.UI.WallReinforcementWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Армирование стен"
        Width="400" Height="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Параметры армирования стен" FontSize="16" FontWeight="Bold" Margin="0,0,0,15"/>

        <!-- Выбор вида -->
        <TextBlock Grid.Row="1" Text="Рабочий вид:" Margin="0,5"/>
        <ComboBox Grid.Row="2" x:Name="CmbWorkView" Margin="0,2,0,10" Height="25"/>

        <!-- Минимальный отступ -->
        <TextBlock Grid.Row="3" Text="Мин. отступ горизонт. арматуры (мм):" Margin="0,5"/>
        <TextBox Grid.Row="4" x:Name="TxtMinOffset" Text="50" Margin="0,2,0,10" Height="25"/>

        <!-- Шаг шпилек -->
        <TextBlock Grid.Row="5" Text="Шаг шпилек (мм):" Margin="0,5"/>
        <TextBox Grid.Row="6" x:Name="TxtStudSpacing" Text="400" Margin="0,2,0,10" Height="25"/>

        <!-- Статус -->
        <TextBlock Grid.Row="7" x:Name="TxtStatus" Text="Выберите стены и нажмите 'Выполнить'"
                   Margin="0,5" TextWrapping="Wrap" Foreground="Gray"/>

        <!-- Кнопки -->
        <StackPanel Grid.Row="8" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button x:Name="BtnExecute" Content="Выполнить" Width="100" Height="30" Margin="0,0,10,0"
                    Click="BtnExecute_Click" IsDefault="True"/>
            <Button Content="Отмена" Width="100" Height="30" IsCancel="True" Click="BtnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
